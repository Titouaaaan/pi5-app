# deploy.sh parks the previous build and node_modules here for rollback,
# and writes info.json describing the current deploy.
/.deploy/

# backend state: visitor counter database and its secret
/.data/

# Python
/backend/.venv/
__pycache__/
.pytest_cache/
